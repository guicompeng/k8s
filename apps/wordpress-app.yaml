apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wordpress
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://charts.bitnami.com/bitnami
    chart: wordpress
    targetRevision: 27.0.10
    helm:
      values: |
        service:
          type: NodePort
          nodePorts:
            http: 30080
            https: 30443

        wordpressUsername: admin
        wordpressPassword: changeme
        wordpressEmail: admin@example.com
        wordpressFirstName: Admin
        wordpressLastName: User
        wordpressBlogName: Meu Blog

        metrics:
          enabled: true

        persistence:
          enabled: true
          size: 4Gi

        mariadb:
          enabled: true
          architecture: standalone
          auth:
            rootPassword: rootpassword
            password: wordpresspassword
          primary:
            persistence:
              enabled: true
              size: 4Gi

          # 👇 Habilita métricas do MariaDB
          metrics:
            enabled: true
            serviceMonitor:
              enabled: true
              namespace: monitoring
              interval: 30s
              scrapeTimeout: 10s
              additionalLabels:
                release: kube-prometheus-stack

        # 👇 Mantém métricas do WordPress se quiser também
        metrics:
          enabled: true
          serviceMonitor:
            enabled: true
            namespace: monitoring
            interval: 30s
            scrapeTimeout: 10s
            additionalLabels:
              release: kube-prometheus-stack

  destination:
    server: https://kubernetes.default.svc
    namespace: wordpress

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
